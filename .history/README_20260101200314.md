# 🎓 Immutable Digital Recognition System

A blockchain-based certificate issuance and verification platform using NFTs. Built with Hardhat 3, Solidity, React, and ethers.js.

## ✨ Features

- **Issue Certificates** - Create tamper-proof digital certificates as NFTs
- **Verify Certificates** - Instantly verify any certificate's authenticity
- **Gallery View** - Browse all issued certificates
- **Admin Panel** - Manage issuers and roles
- **Role-Based Access** - Admin and Issuer roles for access control
- **Revocation** - Revoke certificates when needed

## 🛠️ Tech Stack

- **Smart Contract:** Solidity 0.8.28, OpenZeppelin
- **Blockchain:** Hardhat 3 (Ethereum)
- **Frontend:** React 19, Vite, ethers.js
- **Wallet:** MetaMask

## 📋 Prerequisites

- [Node.js](https://nodejs.org/) (v18 or higher)
- [MetaMask](https://metamask.io/) browser extension
- Git

## 🚀 Quick Start

### 1. Clone the Repository

```bash
git clone <your-repo-url>
cd moti
```

### 2. Install Dependencies

```bash
# Install root dependencies
npm install

# Install frontend dependencies
cd frontend
npm install
cd ..
```

### 3. Start Local Blockchain (Terminal 1)

```bash
npx hardhat node
```

Keep this terminal running! You'll see 20 test accounts with 10,000 ETH each.

### 4. Deploy Smart Contract (Terminal 2)

```bash
npx hardhat run scripts/deploy-localhost.ts --network localhost
```

You should see:
```
✅ Certificate deployed to: 0x5FbDB2315678afecb367f032d93F642f64180aa3
```

### 5. Start Frontend (Terminal 3)

```bash
cd frontend
npm run dev
```

Frontend will be available at: `http://localhost:5173`

> For local development: the frontend will automatically connect to the local node's deployer account (admin) when MetaMask is not connected. You can also manually switch to the local admin via the Admin page by clicking **Use Local Admin** (only in non-production environments).

## 🦊 MetaMask Setup

### Add Hardhat Network

1. Open MetaMask
2. Click network dropdown → "Add Network" → "Add a network manually"
3. Enter:
   - **Network Name:** `Hardhat`
   - **RPC URL:** `http://127.0.0.1:8545`
   - **Chain ID:** `31337`
   - **Currency Symbol:** `ETH`
4. Click Save

### Import Test Account

1. In MetaMask, click account icon → "Add account or hardware wallet" → "Import account"
2. Paste this private key:
   ```
   ac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80
   ```
3. Click Import

> ⚠️ **Warning:** This is a TEST account with a PUBLIC private key. Only use it for local development!

This account is the **Admin/Issuer** and has 10,000 test ETH.

## 🧪 Running Tests

```bash
npx hardhat test --network hardhatMainnet
```

All 14 tests should pass:
- Deployment (3 tests)
- Issuer Management (3 tests)
- Certificate Issuance (3 tests)
- Certificate Verification (3 tests)
- Certificate Revocation (2 tests)

## 📖 Usage Guide

### Issue a Certificate

1. Connect wallet (must be Admin or Issuer)
2. Click "Issue" in navbar
3. Fill in certificate details:
   - Recipient Address
   - Recipient Name
   - Course Name
   - Institution Name
4. Click "Issue Certificate"
5. Confirm in MetaMask

### Verify a Certificate

1. Click "Verify" in navbar
2. Enter Certificate ID (starts from 0)
3. Click "Verify"
4. See verification result and certificate details

### View All Certificates

1. Click "Gallery" in navbar
2. Browse all certificates
3. Filter by: All, Owned, Valid, Revoked
4. Click any certificate for full details

### Admin Functions

1. Click "Admin" in navbar (only visible to admins)
2. Add/Remove issuers
3. Check role status of any address

## 📁 Project Structure

```
moti/
├── contracts/
│   └── Certificate.sol        # Main smart contract
├── test/
│   └── Certificate.ts         # Unit tests
├── scripts/
│   ├── deploy-localhost.ts    # Local deployment script
│   └── deploy-and-test.ts     # Deploy and test script
├── ignition/
│   └── modules/
│       └── Certificate.ts     # Ignition deploy module
├── frontend/
│   ├── src/
│   │   ├── components/        # Navbar, CertificateCard
│   │   ├── pages/             # Home, Issue, Verify, Gallery, Admin
│   │   ├── context/           # Web3 provider
│   │   ├── hooks/             # useWeb3 hook
│   │   └── config/            # Contract ABI & address
│   └── ...
├── hardhat.config.ts          # Hardhat configuration
└── package.json
```

## 🔧 Smart Contract Functions

| Function | Description |
|----------|-------------|
| `issueCertificate()` | Issue a new certificate NFT |
| `verifyCertificate()` | Verify certificate validity |
| `revokeCertificate()` | Revoke a certificate |
| `getCertificateDetails()` | Get full certificate data |
| `addIssuer()` | Add a new issuer (admin only) |
| `removeIssuer()` | Remove an issuer (admin only) |
| `isIssuer()` | Check if address is issuer |
| `isAdmin()` | Check if address is admin |

## 🌐 Deploying to Testnet (Optional)

1. Create `.env` file:
   ```
   SEPOLIA_RPC_URL=your_rpc_url
   SEPOLIA_PRIVATE_KEY=your_private_key
   ```

2. Deploy:
   ```bash
   npx hardhat run scripts/deploy-localhost.ts --network sepolia
   ```

3. Update contract address in `frontend/src/config/contract.js`

## 📄 License

MIT

## 🤝 Contributing

Pull requests are welcome!

---

Built with ❤️ using Hardhat, React, and Solidity
